# Manufacturing Vision Analyzer — Changes Log (Feb 20, 2026)

All modifications made during this session, visualized with Mermaid diagrams.

---

## 1. Overall Architecture (Before vs After)

```mermaid
graph TB
    subgraph BEFORE["BEFORE — Scattered Config"]
        P1["Program.cs<br/>Line 21-24<br/>Vision keys hardcoded"]
        P2["Program.cs<br/>Line ~296<br/>OpenAI keys hardcoded"]
        P3["Program.cs<br/>Line ~723<br/>OpenAI keys duplicated"]
        P4["Program.cs<br/>Line ~835<br/>OpenAI keys duplicated"]
        F1["FlowchartFolderProcessor.cs<br/>Line ~23<br/>Vision keys hardcoded"]
    end

    subgraph AFTER["AFTER — Centralized Config"]
        AS["appsettings.json<br/>Single source of truth"]
        AC["AppConfig.cs<br/>Static config reader"]
        AS -->|loaded by| AC
        AC -->|VisionEndpoint<br/>VisionKey| PP["Program.cs"]
        AC -->|OpenAIEndpoint<br/>OpenAIKey<br/>OpenAIDeployment<br/>OpenAIApiVersion| PP
        AC -->|VisionEndpoint<br/>VisionKey| FF["FlowchartFolderProcessor.cs"]
    end

    BEFORE -.->|refactored to| AFTER

    style BEFORE fill:#ffcccc,stroke:#cc0000
    style AFTER fill:#ccffcc,stroke:#009900
    style AS fill:#ffffcc,stroke:#999900
    style AC fill:#cce5ff,stroke:#0066cc
```

---

## 2. Config Centralization Flow

```mermaid
flowchart LR
    ENV["Environment Variables<br/>(VISION_ENDPOINT, VISION_KEY,<br/>AZURE_OPENAI_ENDPOINT, etc.)"]
    JSON["appsettings.json<br/>AzureVision.Endpoint<br/>AzureVision.Key<br/>AzureOpenAI.Endpoint<br/>AzureOpenAI.Key<br/>AzureOpenAI.DeploymentName<br/>AzureOpenAI.ApiVersion"]
    
    JSON -->|fallback values| AC["AppConfig.cs<br/>(static constructor)"]
    ENV -->|override if set| AC

    AC --> P_MAIN["Program.Main()<br/>Vision client init"]
    AC --> P_FSD["FindSimilarDefects()<br/>OpenAI chat call"]
    AC --> P_SSI["ShowSampleInsights()<br/>OpenAI chat call"]
    AC --> P_FLOW["RunFlowchartFolderModeAsync()<br/>OpenAI chat call"]
    AC --> P_RBG["RebuildGraph()<br/>Vision client init"]
    AC --> FP["FlowchartFolderProcessor<br/>.ProcessSingleImageAsync()<br/>Vision OCR call"]

    style JSON fill:#ffffcc,stroke:#999900
    style ENV fill:#e6ccff,stroke:#6600cc
    style AC fill:#cce5ff,stroke:#0066cc
```

---

## 3. Files Created Today

```mermaid
graph LR
    subgraph NEW["New Files"]
        A["appsettings.json"]
        B["AppConfig.cs"]
    end

    A -->|"6 config values<br/>(2 Vision + 4 OpenAI)"| B
    B -->|"static properties<br/>loaded once at startup"| C["All .cs consumers"]

    style NEW fill:#ccffcc,stroke:#009900
    style A fill:#ffffcc,stroke:#999900
    style B fill:#cce5ff,stroke:#0066cc
```

---

## 4. Files Modified Today

```mermaid
graph TD
    subgraph MODIFIED["Modified Files"]
        direction TB
        P["Program.cs"]
        F["FlowchartFolderProcessor.cs"]
        CSP["ManufacturingVisionAnalyzer.csproj"]
    end

    P -->|"4 locations cleaned"| D1["Removed hardcoded<br/>Vision + OpenAI keys"]
    P -->|"Option 9 cleanup"| D2["Removed fake BUSINESS VALUE<br/>Removed redundant network diagram<br/>Added data-driven summary footer"]
    P -->|"RebuildGraph fix"| D3["Uses AppConfig + GetMVTecPath()<br/>instead of hardcoded paths"]

    F -->|"1 location cleaned"| D4["Removed hardcoded<br/>Vision endpoint + key"]

    CSP -->|"Added item"| D5["CopyToOutputDirectory<br/>for appsettings.json"]

    style MODIFIED fill:#fff3cc,stroke:#cc9900
    style D1 fill:#ffcccc,stroke:#cc0000
    style D2 fill:#ffcccc,stroke:#cc0000
    style D3 fill:#ffcccc,stroke:#cc0000
    style D4 fill:#ffcccc,stroke:#cc0000
    style D5 fill:#ccffcc,stroke:#009900
```

---

## 5. Option 9 Dashboard — Before vs After

```mermaid
graph TB
    subgraph BEFORE_9["BEFORE — Option 9 Dashboard"]
        V1["Defect Type Distribution ✅"]
        V2["Severity Breakdown ✅"]
        V3["Defects By Product ✅"]
        V4["Equipment Usage ✅"]
        V5["Quality Heatmap ✅"]
        V6["Cross-Product Network Diagram ❌<br/>(redundant with Option 6)"]
        V7["BUSINESS VALUE ❌<br/>(hardcoded fake stats:<br/>10x faster, 70%, $50K+)"]
    end

    subgraph AFTER_9["AFTER — Option 9 Dashboard"]
        W1["Defect Type Distribution ✅"]
        W2["Severity Breakdown ✅"]
        W3["Defects By Product ✅"]
        W4["Equipment Usage ✅"]
        W5["Quality Heatmap ✅"]
        W6["Data-Driven Summary Footer ✅<br/>(real counts from graph)"]
    end

    V6 -.->|removed| X1["Use Option 6 instead"]
    V7 -.->|replaced| W6

    style BEFORE_9 fill:#ffcccc,stroke:#cc0000
    style AFTER_9 fill:#ccffcc,stroke:#009900
    style V6 fill:#ff9999,stroke:#cc0000
    style V7 fill:#ff9999,stroke:#cc0000
    style W6 fill:#99ff99,stroke:#009900
```

---

## 6. Hardcoded Values Eliminated

```mermaid
graph LR
    subgraph REMOVED["Removed from Source Code"]
        K1["https://mfg-vision-demo<br/>.cognitiveservices.azure.com/"]
        K2["5RyzIui4JOKfJNx...<br/>(Vision API Key)"]
        K3["https://mfg-demo<br/>.openai.azure.com"]
        K4["A2AhC6XdGWJPUA...<br/>(OpenAI API Key)"]
        K5["gpt-4.1<br/>(Deployment Name)"]
        K6["2025-01-01-preview<br/>(API Version)"]
        K7["YOUR_ENDPOINT_HERE<br/>(RebuildGraph fallback)"]
        K8["C:\\Users\\rishah\\mvtec...<br/>(hardcoded path)"]
        K9["10x faster decision-making<br/>70% documentation reduction<br/>$50K+ optimization<br/>(fake statistics)"]
    end

    subgraph MOVED["Moved To"]
        J1["appsettings.json"]
        J2["AppConfig.cs"]
    end

    K1 & K2 & K3 & K4 & K5 & K6 -->|centralized| J1
    K7 & K8 -->|replaced with| J2
    K9 -->|deleted| GONE["Replaced by real<br/>graph-computed stats"]

    style REMOVED fill:#ffcccc,stroke:#cc0000
    style MOVED fill:#ccffcc,stroke:#009900
    style GONE fill:#e6e6e6,stroke:#999999
```

---

## 7. AppConfig Priority Chain

```mermaid
flowchart TD
    A["App Starts"] --> B{"Environment variable<br/>set?"}
    B -->|Yes| C["Use env var value"]
    B -->|No| D{"appsettings.json<br/>found?"}
    D -->|Yes| E["Use JSON value"]
    D -->|No| F["Empty string<br/>(will fail gracefully)"]

    style A fill:#cce5ff,stroke:#0066cc
    style C fill:#ccffcc,stroke:#009900
    style E fill:#ffffcc,stroke:#999900
    style F fill:#ffcccc,stroke:#cc0000
```

---

## 8. Summary of All Changes

| # | Change | File(s) | Type |
|---|--------|---------|------|
| 1 | Created centralized config file | `appsettings.json` | New |
| 2 | Created static config reader class | `AppConfig.cs` | New |
| 3 | Replaced 4 hardcoded key/endpoint blocks | `Program.cs` | Modified |
| 4 | Replaced 1 hardcoded key/endpoint block | `FlowchartFolderProcessor.cs` | Modified |
| 5 | Added CopyToOutputDirectory for config | `ManufacturingVisionAnalyzer.csproj` | Modified |
| 6 | Removed fake BUSINESS VALUE stats | `Program.cs` (Option 9) | Modified |
| 7 | Removed redundant network diagram | `Program.cs` (Option 9) | Modified |
| 8 | Added data-driven summary footer | `Program.cs` (Option 9) | Modified |
| 9 | Fixed RebuildGraph to use AppConfig | `Program.cs` (Option 11) | Modified |
| 10 | Fixed RebuildGraph to use GetMVTecPath() | `Program.cs` (Option 11) | Modified |

**Zero hardcoded API keys, endpoints, or fake statistics remain in any .cs file.**
